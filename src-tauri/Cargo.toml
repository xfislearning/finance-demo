[package]
name = "qentro-finance"
version = "0.5.1"
description = "Local-first bookkeeping for small businesses"
authors = ["Qentro"]
edition = "2021"

[lib]
name = "qentro_finance_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
